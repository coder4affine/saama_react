// import React from 'react';
// import { render, fireEvent, waitFor, screen } from '@testing-library/react';
// import TodoActions from './todoActions';

describe('test todoActions Component', () => {
  it('check number of buttons available in component', () => {
    expect(true).toBe(true);
    // const props = {
    //   changeStatus: jest.fn(),
    // };

    // const { getByTestId } = render(<TodoActions {...props} />);
    // // const buttons = container.querySelector('#btn-all');
    // // console.warn(buttons);
    // console.warn(getByTestId('btn-all').r);
    // // expect(findByTestId('btn-all').props.children).toStrictEqual('All');
  });
});

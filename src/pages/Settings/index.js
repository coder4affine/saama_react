import React from 'react';
// import PropTypes from 'prop-types';

const Index = (props) => {
  console.warn(props);
  return (
    <div>
      <h1>Settings Page</h1>
    </div>
  );
};

Index.propTypes = {};

export default Index;

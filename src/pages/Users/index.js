import React from 'react';
// import PropTypes from 'prop-types';

const Index = (props) => {
  console.warn('props', props);
  return (
    <div>
      <h1>Users Page</h1>
    </div>
  );
};

Index.propTypes = {};

export default Index;
